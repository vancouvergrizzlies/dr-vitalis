{
  "mcpServers": {
    "council": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.py"],
      "env": {
        "COUNCIL_DB_PATH": "${HOME}/.health-jarvis/council.db",
        "COUNCIL_DASHBOARD_PATH": "${HOME}/.health-jarvis/dashboard.html",
        "X_BEARER_TOKEN": "${X_BEARER_TOKEN}"
      }
    }
  }
}
